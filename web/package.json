{
  "name": "pyxis",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "pnpm --filter pyxis-user-site dev",
    "build": "pnpm --filter pyxis-user-site build",
    "preview": "pnpm --filter pyxis-user-site preview",
    "storybook": "pnpm --filter pyxis-components storybook",
    "storybook:user-site": "pnpm --filter pyxis-user-site storybook",
    "build-storybook:user-site": "pnpm --filter pyxis-user-site build-storybook",
    "build:components": "pnpm --filter pyxis-components build",
    "build:components:watch": "pnpm --filter pyxis-components build:watch",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "test:watch": "pnpm -r test:watch",
    "typecheck": "pnpm -r typecheck",
    "format": "pnpm -r format",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "@types/node": "^25.7.0",
    "playwright": "^1.60.0",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.0.0"
}
