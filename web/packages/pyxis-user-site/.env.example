# API base URL for the Pyxis backend
# In production (Vercel/Netlify): set via dashboard — do NOT commit real values
VITE_API_URL=https://api.pyxis.xyz

# Optional: Analytics (Vercel Analytics, Plausible, etc.)
# VITE_ANALYTICS_ID=
