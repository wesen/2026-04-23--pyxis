# Experiments

One-off or interrupted tuning work lives here so active source paths can be kept clean.

## 01-interrupted-shows-fixture-spacing-pass.patch

Patch captured before pivoting from Shows tuning into writing the full app end-to-end workflow guide. It contains experimental page gutter/header spacing and Shows fixture/poster-data alignment changes. It was intentionally not left applied because it was an incomplete tuning pass and briefly caused type-check friction around `AgeRestriction`/generated declaration drift.
