# Generated by scripts/11-generate-prototype-baseline-configs.mjs.
# Purpose: prototype baseline extraction for the Pyxis public site pages.
metadata:
  slug: "prototype-public-about"
  title: "Prototype Public Site / About baseline"
  source_html: "prototype-design/Pyxis Public Site.html"
  source_page: "about"
  description: "Standalone public about page, desktop."

original:
  name: "prototype-public-about"
  url: "http://localhost:7070/standalone/public/about.html"
  wait_ms: 0
  viewport: { width: 920, height: 1400 }
  root_selector: "#root"
  prepare:
    type: script
    wait_for: "document.querySelector('#root > *:first-child') && (!document.fonts || document.fonts.status === 'loaded')"
    script: |
      const root = document.querySelector('#root');
      if (root) {
        root.style.minHeight = '0px';
        root.style.height = 'auto';
      }
      document.body.style.margin = '0';
    after_wait_ms: 250

react:
  name: "prototype-public-about"
  url: "http://localhost:7070/standalone/public/about.html"
  wait_ms: 0
  viewport: { width: 920, height: 1400 }
  root_selector: "#root"
  prepare:
    type: script
    wait_for: "document.querySelector('#root > *:first-child') && (!document.fonts || document.fonts.status === 'loaded')"
    script: |
      const root = document.querySelector('#root');
      if (root) {
        root.style.minHeight = '0px';
        root.style.height = 'auto';
      }
      document.body.style.margin = '0';
    after_wait_ms: 250

styles:
  - name: "full-shell"
    selector: "#root"
    include_bounds: true
    props: [display, position, box-sizing, width, height, min-width, min-height, max-width, max-height, margin, padding, gap, font-family, font-size, font-weight, line-height, letter-spacing, text-transform, text-align, color, background, background-color, border, border-color, border-width, border-style, border-radius, box-shadow, opacity, align-items, justify-content, grid-template-columns, flex-direction, white-space, overflow]
  - name: "shell"
    selector: "#root > div"
    include_bounds: true
    props: [display, position, box-sizing, width, height, min-width, min-height, max-width, max-height, margin, padding, gap, font-family, font-size, font-weight, line-height, letter-spacing, text-transform, text-align, color, background, background-color, border, border-color, border-width, border-style, border-radius, box-shadow, opacity, align-items, justify-content, grid-template-columns, flex-direction, white-space, overflow]
  - name: "nav"
    selector: "#root > div > header"
    include_bounds: true
    props: [display, width, height, padding, font-family, font-size, font-weight, line-height, color, background-color, border, border-radius]
  - name: "about-header"
    selector: "#root > div > main > :first-child"
    include_bounds: true
    props: [display, width, height, margin, padding, font-family, font-size, font-weight, line-height, color]
  - name: "about-intro"
    selector: "#root > div > main > :nth-child(3)"
    include_bounds: true
    props: [display, width, height, margin, padding, font-family, font-size, font-style, line-height, color]
  - name: "about-ethos"
    selector: "#root > div > main > :nth-child(4)"
    include_bounds: true
    props: [display, width, height, margin, padding, grid-template-columns, border-top]
  - name: "about-collective"
    selector: "#root > div > main > :nth-child(5)"
    include_bounds: true
    props: [display, width, height, margin, padding, font-family, font-size, color, border-top]
  - name: "main"
    selector: "#root > div > main"
    include_bounds: true
    props: [display, width, height, margin, padding, font-family, font-size, color, background-color]
  - name: "footer"
    selector: "#root > div > footer"
    include_bounds: true
    props: [display, width, height, margin, padding, font-family, font-size, color, background-color, border]

output:
  dir: "/home/manuel/code/wesen/2026-04-23--pyxis/ttmp/2026/04/23/PYXIS-STORYBOOK-CATALOG--build-storybook-screenshot-and-css-catalog-for-atoms-molecules-and-public-components/various/prototype-baseline/artifacts/public/about"
  write_json: true
  write_markdown: true
  write_pngs: true
  write_prepared_html: true
  write_inspect_json: true
  validate_pngs: true

modes: [capture]
