version: "2"

run:
  timeout: 5m
  tests: true

linters:
  enable:
    - govet
    - ineffassign
    - staticcheck
    - unused

issues:
  max-same-issues: 20
  exclude-dirs:
    - ttmp
    - web
    - prototype-design
    - gen
