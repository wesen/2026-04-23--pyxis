# Generated by scripts/11-generate-prototype-baseline-configs.mjs.
# Purpose: prototype baseline extraction for the Pyxis public site pages.
metadata:
  slug: "prototype-public-book-mobile"
  title: "Prototype Public Site / Book baseline / Mobile"
  source_html: "prototype-design/Pyxis Public Site.html"
  source_page: "book"
  description: "Standalone public booking page, mobile."

original:
  name: "prototype-public-book-mobile"
  url: "http://localhost:7070/standalone/public/book-mobile.html"
  wait_ms: 0
  viewport: { width: 390, height: 1880 }
  root_selector: "#root"
  prepare:
    type: script
    wait_for: "document.querySelector('#root > *:first-child') && (!document.fonts || document.fonts.status === 'loaded')"
    script: |
      const root = document.querySelector('#root');
      if (root) {
        root.style.minHeight = '0px';
        root.style.height = 'auto';
      }
      document.body.style.margin = '0';
    after_wait_ms: 250

react:
  name: "prototype-public-book-mobile"
  url: "http://localhost:7070/standalone/public/book-mobile.html"
  wait_ms: 0
  viewport: { width: 390, height: 1880 }
  root_selector: "#root"
  prepare:
    type: script
    wait_for: "document.querySelector('#root > *:first-child') && (!document.fonts || document.fonts.status === 'loaded')"
    script: |
      const root = document.querySelector('#root');
      if (root) {
        root.style.minHeight = '0px';
        root.style.height = 'auto';
      }
      document.body.style.margin = '0';
    after_wait_ms: 250

styles:
  - name: "full-shell"
    selector: "#root"
    include_bounds: true
    props: [display, width, height, margin, padding, gap, font-family, font-size, font-weight, line-height, color, background-color, border, border-radius, box-shadow, grid-template-columns, justify-content, align-items]
  - name: "shell"
    selector: "#root > div"
    include_bounds: true
    props: [display, width, height, margin, padding, gap, font-family, font-size, font-weight, line-height, color, background-color, border, border-radius, box-shadow, grid-template-columns, justify-content, align-items]
  - name: "nav"
    selector: "#root > div > header"
    include_bounds: true
    props: [display, width, height, padding, font-family, font-size, font-weight, line-height, color, background-color, border, border-radius]
  - name: "book-header"
    selector: "#root > div > main > :first-child"
    include_bounds: true
    props: [display, width, height, margin, padding, font-family, font-size, font-weight, line-height, color]
  - name: "book-layout"
    selector: "#root > div > main > :nth-child(3)"
    include_bounds: true
    props: [display, width, height, gap, grid-template-columns, align-items]
  - name: "book-form"
    selector: "#root > div > main > :nth-child(3) > form"
    include_bounds: true
    props: [display, width, height, gap, padding, font-family, font-size, color]
  - name: "book-info"
    selector: "#root > div > main > :nth-child(3) > aside"
    include_bounds: true
    props: [display, width, height, padding, background-color, border-radius, color]
  - name: "main"
    selector: "#root > div > main"
    include_bounds: true
    props: [display, width, height, margin, padding, font-family, font-size, color, background-color]
  - name: "footer"
    selector: "#root > div > footer"
    include_bounds: true
    props: [display, width, height, margin, padding, font-family, font-size, color, background-color, border]

output:
  dir: "/home/manuel/code/wesen/2026-04-23--pyxis/prototype-design/baseline/artifacts/public/book-mobile"
  write_json: true
  write_markdown: true
  write_pngs: true
  write_prepared_html: true
  write_inspect_json: true
  validate_pngs: true

modes: [capture]
