# Generated by scripts/11-generate-prototype-baseline-configs.mjs.
# Purpose: prototype baseline extraction for the Pyxis public site pages.
metadata:
  slug: "prototype-public-detail"
  title: "Prototype Public Site / Show Detail baseline"
  source_html: "prototype-design/Pyxis Public Site.html"
  source_page: "detail"
  description: "Standalone public show detail page, desktop."

original:
  name: "prototype-public-detail"
  url: "http://localhost:7070/standalone/public/detail.html"
  wait_ms: 0
  viewport: { width: 920, height: 1300 }
  root_selector: "#root"
  prepare:
    type: script
    wait_for: "document.querySelector('#root > *:first-child') && (!document.fonts || document.fonts.status === 'loaded')"
    script: |
      const root = document.querySelector('#root');
      if (root) {
        root.style.minHeight = '0px';
        root.style.height = 'auto';
      }
      document.body.style.margin = '0';
    after_wait_ms: 250

react:
  name: "prototype-public-detail"
  url: "http://localhost:7070/standalone/public/detail.html"
  wait_ms: 0
  viewport: { width: 920, height: 1300 }
  root_selector: "#root"
  prepare:
    type: script
    wait_for: "document.querySelector('#root > *:first-child') && (!document.fonts || document.fonts.status === 'loaded')"
    script: |
      const root = document.querySelector('#root');
      if (root) {
        root.style.minHeight = '0px';
        root.style.height = 'auto';
      }
      document.body.style.margin = '0';
    after_wait_ms: 250

styles:
  - name: "full-shell"
    selector: "#root"
    include_bounds: true
    props: [display, position, box-sizing, width, height, min-width, min-height, max-width, max-height, margin, padding, gap, font-family, font-size, font-weight, line-height, letter-spacing, text-transform, text-align, color, background, background-color, border, border-color, border-width, border-style, border-radius, box-shadow, opacity, align-items, justify-content, grid-template-columns, flex-direction, white-space, overflow]
  - name: "shell"
    selector: "#root > div"
    include_bounds: true
    props: [display, position, box-sizing, width, height, min-width, min-height, max-width, max-height, margin, padding, gap, font-family, font-size, font-weight, line-height, letter-spacing, text-transform, text-align, color, background, background-color, border, border-color, border-width, border-style, border-radius, box-shadow, opacity, align-items, justify-content, grid-template-columns, flex-direction, white-space, overflow]
  - name: "nav"
    selector: "#root > div > header"
    include_bounds: true
    props: [display, width, height, padding, font-family, font-size, font-weight, line-height, color, background-color, border, border-radius]
  - name: "back-link"
    selector: "#root > div > main > :first-child"
    include_bounds: true
    props: [display, width, height, margin, padding, font-family, font-size, color]
  - name: "detail-grid"
    selector: "#root > div > main > :nth-child(2)"
    include_bounds: true
    props: [display, width, height, gap, grid-template-columns, align-items]
  - name: "poster-strip"
    selector: "#root > div > main > :nth-child(2) > div:first-child"
    include_bounds: true
    props: [display, width, height, margin, padding, background-color, border, border-radius]
  - name: "ticket-card"
    selector: "#root > div > main > :nth-child(2) > div:first-child > div:last-child"
    include_bounds: true
    props: [display, width, height, margin, padding, background-color, border, border-radius, box-shadow]
  - name: "detail-body"
    selector: "#root > div > main > :nth-child(2) > div:last-child"
    include_bounds: true
    props: [display, width, height, margin, padding, font-family, font-size, color, background-color]
  - name: "main"
    selector: "#root > div > main"
    include_bounds: true
    props: [display, width, height, margin, padding, font-family, font-size, color, background-color]
  - name: "footer"
    selector: "#root > div > footer"
    include_bounds: true
    props: [display, width, height, margin, padding, font-family, font-size, color, background-color, border]

output:
  dir: "/home/manuel/code/wesen/2026-04-23--pyxis/prototype-design/baseline/artifacts/public/detail"
  write_json: true
  write_markdown: true
  write_pngs: true
  write_prepared_html: true
  write_inspect_json: true
  validate_pngs: true

modes: [capture]
