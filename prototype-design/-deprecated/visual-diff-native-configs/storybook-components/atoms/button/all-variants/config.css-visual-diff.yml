# Generated by prototype-design/visual-diff/scripts/18-generate-storybook-design-system-configs.mjs.
# Purpose: Storybook implementation catalog extraction for Pyxis design-system stories.
metadata:
  slug: "atoms-button--all-variants"
  title: "Atoms/Button"
  story_name: "All Variants"
  story_id: "atoms-button--all-variants"
  import_path: "./src/atoms/Button/Button.stories.tsx"
  component_path: "./src/atoms/Button/Button.tsx"
  source: "web/packages/pyxis-components/storybook-static/index.json"

original:
  name: "atoms-button--all-variants"
  url: "http://localhost:6006/iframe.html?id=atoms-button--all-variants&viewMode=story"
  wait_ms: 0
  viewport: { width: 960, height: 720 }
  root_selector: "#storybook-root"
  prepare:
    type: script
    wait_for: "document.querySelector('#storybook-root > *:first-child') && (!document.fonts || document.fonts.status === 'loaded')"
    script: |
      document.body.style.margin = '0';
      document.documentElement.style.background = '#f3f1eb';
      const root = document.querySelector('#storybook-root');
      if (root) {
        root.style.minHeight = '0px';
      }
    after_wait_ms: 250

react:
  name: "atoms-button--all-variants"
  url: "http://localhost:6006/iframe.html?id=atoms-button--all-variants&viewMode=story"
  wait_ms: 0
  viewport: { width: 960, height: 720 }
  root_selector: "#storybook-root"
  prepare:
    type: script
    wait_for: "document.querySelector('#storybook-root > *:first-child') && (!document.fonts || document.fonts.status === 'loaded')"
    script: |
      document.body.style.margin = '0';
      document.documentElement.style.background = '#f3f1eb';
      const root = document.querySelector('#storybook-root');
      if (root) {
        root.style.minHeight = '0px';
      }
    after_wait_ms: 250

styles:
  - name: "story-root"
    selector: "#storybook-root"
    include_bounds: true
    props: [display, position, box-sizing, width, height, min-width, min-height, max-width, max-height, margin, padding, gap, font-family, font-size, font-weight, line-height, letter-spacing, text-transform, text-align, color, background, background-color, border, border-color, border-width, border-style, border-radius, box-shadow, opacity, align-items, justify-content, grid-template-columns, flex-direction, white-space, overflow]
  - name: "capture-target"
    selector: "#storybook-root [data-pyxis-story-root], #storybook-root > *:first-child:not(:has([data-pyxis-story-root]))"
    include_bounds: true
    props: [display, position, box-sizing, width, height, min-width, min-height, max-width, max-height, margin, padding, gap, font-family, font-size, font-weight, line-height, letter-spacing, text-transform, text-align, color, background, background-color, border, border-color, border-width, border-style, border-radius, box-shadow, opacity, align-items, justify-content, grid-template-columns, flex-direction, white-space, overflow]

output:
  dir: "/home/manuel/code/wesen/2026-04-23--pyxis/prototype-design/storybook-catalog/artifacts/atoms/button/all-variants"
  write_json: true
  write_markdown: true
  write_pngs: true
  write_prepared_html: true
  write_inspect_json: true
  validate_pngs: true

modes: [capture]
