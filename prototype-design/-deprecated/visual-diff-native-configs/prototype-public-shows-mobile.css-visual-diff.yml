# Generated by scripts/11-generate-prototype-baseline-configs.mjs.
# Purpose: prototype baseline extraction for the Pyxis public site pages.
metadata:
  slug: "prototype-public-shows-mobile"
  title: "Prototype Public Site / Shows baseline / Mobile"
  source_html: "prototype-design/Pyxis Public Site.html"
  source_page: "shows"
  description: "Standalone public Shows page, mobile."

original:
  name: "prototype-public-shows-mobile"
  url: "http://localhost:7070/standalone/public/shows-mobile.html"
  wait_ms: 0
  viewport: { width: 390, height: 1960 }
  root_selector: "#root"
  prepare:
    type: script
    wait_for: "document.querySelector('#root > *:first-child') && (!document.fonts || document.fonts.status === 'loaded')"
    script: |
      const root = document.querySelector('#root');
      if (root) {
        root.style.minHeight = '0px';
        root.style.height = 'auto';
      }
      document.body.style.margin = '0';
    after_wait_ms: 250

react:
  name: "prototype-public-shows-mobile"
  url: "http://localhost:7070/standalone/public/shows-mobile.html"
  wait_ms: 0
  viewport: { width: 390, height: 1960 }
  root_selector: "#root"
  prepare:
    type: script
    wait_for: "document.querySelector('#root > *:first-child') && (!document.fonts || document.fonts.status === 'loaded')"
    script: |
      const root = document.querySelector('#root');
      if (root) {
        root.style.minHeight = '0px';
        root.style.height = 'auto';
      }
      document.body.style.margin = '0';
    after_wait_ms: 250

styles:
  - name: "full-shell"
    selector: "#root"
    include_bounds: true
    props: [display, width, height, margin, padding, gap, font-family, font-size, font-weight, line-height, color, background-color, border, border-radius, box-shadow, grid-template-columns, justify-content, align-items]
  - name: "shell"
    selector: "#root > div"
    include_bounds: true
    props: [display, width, height, margin, padding, gap, font-family, font-size, font-weight, line-height, color, background-color, border, border-radius, box-shadow, grid-template-columns, justify-content, align-items]
  - name: "nav"
    selector: "#root > div > header"
    include_bounds: true
    props: [display, width, height, padding, font-family, font-size, font-weight, line-height, color, background-color, border, border-radius]
  - name: "page-header"
    selector: "#root > div > main > :first-child"
    include_bounds: true
    props: [display, width, height, margin, padding, font-family, font-size, font-weight, line-height, color, background-color]
  - name: "show-grid"
    selector: "#root > div > main > :nth-child(3)"
    include_bounds: true
    props: [display, width, height, gap, grid-template-columns, margin, padding]
  - name: "first-show-tile"
    selector: "#root > div > main > :nth-child(3) > div:first-child"
    include_bounds: true
    props: [display, width, height, margin, padding, font-family, font-size, color, background-color, border, border-radius]
  - name: "first-poster"
    selector: "#root > div > main > :nth-child(3) > div:first-child > div:first-child"
    include_bounds: true
    props: [display, width, height, background-color, border-radius, overflow]
  - name: "main"
    selector: "#root > div > main"
    include_bounds: true
    props: [display, width, height, margin, padding, font-family, font-size, color, background-color]
  - name: "footer"
    selector: "#root > div > footer"
    include_bounds: true
    props: [display, width, height, margin, padding, font-family, font-size, color, background-color, border]

output:
  dir: "/home/manuel/code/wesen/2026-04-23--pyxis/prototype-design/baseline/artifacts/public/shows-mobile"
  write_json: true
  write_markdown: true
  write_pngs: true
  write_prepared_html: true
  write_inspect_json: true
  validate_pngs: true

modes: [capture]
