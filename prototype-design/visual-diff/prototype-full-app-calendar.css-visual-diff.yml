# Generated by scripts/11-generate-prototype-baseline-configs.mjs.
# Purpose: prototype baseline extraction for the Pyxis public site pages.
metadata:
  slug: "prototype-full-app-calendar"
  title: "Prototype Full App / Calendar"
  source_html: "prototype-design/Pyxis Full App.html"
  source_page: "calendar"
  description: "Standalone Full App calendar screen."

original:
  name: "prototype-full-app-calendar"
  url: "http://localhost:7070/standalone/full-app/calendar.html"
  wait_ms: 0
  viewport: { width: 920, height: 960 }
  root_selector: "#root"
  prepare:
    type: script
    wait_for: "document.querySelector('#root > *:first-child') && (!document.fonts || document.fonts.status === 'loaded')"
    script: |
      const root = document.querySelector('#root');
      if (root) {
        root.style.minHeight = '0px';
        root.style.height = 'auto';
      }
      document.body.style.margin = '0';
    after_wait_ms: 250

react:
  name: "prototype-full-app-calendar"
  url: "http://localhost:7070/standalone/full-app/calendar.html"
  wait_ms: 0
  viewport: { width: 920, height: 960 }
  root_selector: "#root"
  prepare:
    type: script
    wait_for: "document.querySelector('#root > *:first-child') && (!document.fonts || document.fonts.status === 'loaded')"
    script: |
      const root = document.querySelector('#root');
      if (root) {
        root.style.minHeight = '0px';
        root.style.height = 'auto';
      }
      document.body.style.margin = '0';
    after_wait_ms: 250

styles:
  - name: "full-screen"
    selector: "#root"
    include_bounds: true
    props: [display, width, height, margin, padding, gap, font-family, font-size, font-weight, line-height, color, background-color, border, border-radius, box-shadow, grid-template-columns, justify-content, align-items]
  - name: "screen-root"
    selector: "#root > div"
    include_bounds: true
    props: [display, position, box-sizing, width, height, min-width, min-height, max-width, max-height, margin, padding, gap, font-family, font-size, font-weight, line-height, letter-spacing, text-transform, text-align, color, background, background-color, border, border-color, border-width, border-style, border-radius, box-shadow, opacity, align-items, justify-content, grid-template-columns, flex-direction, white-space, overflow]
  - name: "sidebar"
    selector: "#root > div > aside"
    include_bounds: true
    props: [display, width, height, padding, font-family, font-size, color, background-color, border]
  - name: "main"
    selector: "#root > div > main"
    include_bounds: true
    props: [display, width, height, padding, gap, font-family, font-size, line-height, color, background-color, overflow]
  - name: "main-header"
    selector: "#root > div > main > div:first-child"
    include_bounds: true
    props: [display, width, height, padding, gap, font-family, font-size, font-weight, line-height, color]
  - name: "month-card"
    selector: "#root > div > main > div:nth-child(2)"
    include_bounds: true
    props: [display, width, height, padding, gap, background-color, border, border-radius]
  - name: "calendar-layout"
    selector: "#root > div > main > div:nth-child(2)"
    include_bounds: true
    props: [display, width, height, gap, grid-template-columns]
  - name: "calendar-card"
    selector: "#root > div > main > div:nth-child(2) > div:first-child"
    include_bounds: true
    props: [display, width, height, padding, background-color, border, border-radius]

output:
  dir: "/home/manuel/code/wesen/2026-04-23--pyxis/prototype-design/baseline/artifacts/full-app/calendar"
  write_json: true
  write_markdown: true
  write_pngs: true
  write_prepared_html: true
  write_inspect_json: true
  validate_pngs: true

modes: [capture]
