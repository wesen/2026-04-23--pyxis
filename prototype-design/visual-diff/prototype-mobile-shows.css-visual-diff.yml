# Generated by scripts/11-generate-prototype-baseline-configs.mjs.
# Purpose: prototype baseline extraction for the Pyxis public site pages.
metadata:
  slug: "prototype-mobile-shows"
  title: "Prototype Mobile App / Shows"
  source_html: "prototype-design/Pyxis Mobile.html"
  source_page: "shows"
  description: "Standalone mobile shows index screen."

original:
  name: "prototype-mobile-shows"
  url: "http://localhost:7070/standalone/mobile/shows.html"
  wait_ms: 0
  viewport: { width: 390, height: 1024 }
  root_selector: "#root"
  prepare:
    type: script
    wait_for: "document.querySelector('#root > *:first-child') && (!document.fonts || document.fonts.status === 'loaded')"
    script: |
      const root = document.querySelector('#root');
      if (root) {
        root.style.minHeight = '0px';
        root.style.height = 'auto';
      }
      document.body.style.margin = '0';
    after_wait_ms: 250

react:
  name: "prototype-mobile-shows"
  url: "http://localhost:7070/standalone/mobile/shows.html"
  wait_ms: 0
  viewport: { width: 390, height: 1024 }
  root_selector: "#root"
  prepare:
    type: script
    wait_for: "document.querySelector('#root > *:first-child') && (!document.fonts || document.fonts.status === 'loaded')"
    script: |
      const root = document.querySelector('#root');
      if (root) {
        root.style.minHeight = '0px';
        root.style.height = 'auto';
      }
      document.body.style.margin = '0';
    after_wait_ms: 250

styles:
  - name: "full-screen"
    selector: "#root"
    include_bounds: true
    props: [display, width, height, margin, padding, gap, font-family, font-size, font-weight, line-height, color, background-color, border, border-radius, box-shadow, grid-template-columns, justify-content, align-items]
  - name: "screen-shell"
    selector: "#root > div"
    include_bounds: true
    props: [display, position, box-sizing, width, height, min-width, min-height, max-width, max-height, margin, padding, gap, font-family, font-size, font-weight, line-height, letter-spacing, text-transform, text-align, color, background, background-color, border, border-color, border-width, border-style, border-radius, box-shadow, opacity, align-items, justify-content, grid-template-columns, flex-direction, white-space, overflow]
  - name: "status-bar"
    selector: "#root > div > div:first-child"
    include_bounds: true
    props: [display, width, height, padding, color, background-color]
  - name: "screen-body"
    selector: "#root > div > div:nth-child(2)"
    include_bounds: true
    props: [display, width, height, padding, gap, background-color, color]
  - name: "nav"
    selector: "#root > div > div:nth-child(2) > div:first-child"
    include_bounds: true
    props: [display, width, height, padding, font-family, font-size, color]
  - name: "hero"
    selector: "#root > div > div:nth-child(2) > div:nth-child(2)"
    include_bounds: true
    props: [display, width, height, padding, font-family, font-size, line-height, color]
  - name: "filters"
    selector: "#root > div > div:nth-child(2) > div:nth-child(3)"
    include_bounds: true
    props: [display, width, height, padding, gap, background-color]
  - name: "first-show-row"
    selector: "#root > div > div:nth-child(2) > div:nth-child(4) > div:first-child"
    include_bounds: true
    props: [display, width, height, padding, gap, background-color, border, border-radius, color]
  - name: "tab-bar"
    selector: "#root > div > div:nth-child(2) > div:last-child"
    include_bounds: true
    props: [display, width, height, padding, background-color, border-top, color]

output:
  dir: "/home/manuel/code/wesen/2026-04-23--pyxis/prototype-design/baseline/artifacts/mobile/shows"
  write_json: true
  write_markdown: true
  write_pngs: true
  write_prepared_html: true
  write_inspect_json: true
  validate_pngs: true

modes: [capture]
