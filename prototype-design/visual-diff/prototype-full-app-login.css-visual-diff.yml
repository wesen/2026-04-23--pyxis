# Generated by scripts/11-generate-prototype-baseline-configs.mjs.
# Purpose: prototype baseline extraction for the Pyxis public site pages.
metadata:
  slug: "prototype-full-app-login"
  title: "Prototype Full App / Login"
  source_html: "prototype-design/Pyxis Full App.html"
  source_page: "login"
  description: "Standalone Full App login screen; foundations excluded."

original:
  name: "prototype-full-app-login"
  url: "http://localhost:7070/standalone/full-app/login.html"
  wait_ms: 0
  viewport: { width: 920, height: 960 }
  root_selector: "#root"
  prepare:
    type: script
    wait_for: "document.querySelector('#root > *:first-child') && (!document.fonts || document.fonts.status === 'loaded')"
    script: |
      const root = document.querySelector('#root');
      if (root) {
        root.style.minHeight = '0px';
        root.style.height = 'auto';
      }
      document.body.style.margin = '0';
    after_wait_ms: 250

react:
  name: "prototype-full-app-login"
  url: "http://localhost:7070/standalone/full-app/login.html"
  wait_ms: 0
  viewport: { width: 920, height: 960 }
  root_selector: "#root"
  prepare:
    type: script
    wait_for: "document.querySelector('#root > *:first-child') && (!document.fonts || document.fonts.status === 'loaded')"
    script: |
      const root = document.querySelector('#root');
      if (root) {
        root.style.minHeight = '0px';
        root.style.height = 'auto';
      }
      document.body.style.margin = '0';
    after_wait_ms: 250

styles:
  - name: "full-screen"
    selector: "#root"
    include_bounds: true
    props: [display, width, height, margin, padding, gap, font-family, font-size, font-weight, line-height, color, background-color, border, border-radius, box-shadow, grid-template-columns, justify-content, align-items]
  - name: "screen-root"
    selector: "#root > div"
    include_bounds: true
    props: [display, position, box-sizing, width, height, min-width, min-height, max-width, max-height, margin, padding, gap, font-family, font-size, font-weight, line-height, letter-spacing, text-transform, text-align, color, background, background-color, border, border-color, border-width, border-style, border-radius, box-shadow, opacity, align-items, justify-content, grid-template-columns, flex-direction, white-space, overflow]
  - name: "brand-panel"
    selector: "#root > div > div:first-child"
    include_bounds: true
    props: [display, width, height, padding, font-family, font-size, line-height, color, background-color, border, border-radius]
  - name: "auth-panel"
    selector: "#root > div > div:nth-child(2)"
    include_bounds: true
    props: [display, width, height, padding, font-family, font-size, line-height, color, background-color, border]
  - name: "welcome-heading"
    selector: "#root > div > div:nth-child(2) h1"
    include_bounds: true
    props: [display, width, height, margin, font-family, font-size, font-weight, line-height, color]
  - name: "discord-button"
    selector: "#root > div > div:nth-child(2) button"
    include_bounds: true
    props: [display, width, height, padding, font-family, font-size, font-weight, color, background-color, border, border-radius]

output:
  dir: "/home/manuel/code/wesen/2026-04-23--pyxis/prototype-design/baseline/artifacts/full-app/login"
  write_json: true
  write_markdown: true
  write_pngs: true
  write_prepared_html: true
  write_inspect_json: true
  validate_pngs: true

modes: [capture]
